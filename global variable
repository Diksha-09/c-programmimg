#include <stdio.h>
int global_testvar = 25;
int addfunction(int number)
{
     global_testvar = number;
}
int main()
{
      int test_var = 30;
      char text[] = "Test message \n";
      addfunction(40);
      printf("%d \n" , global_testvar);
      return 0;
}
